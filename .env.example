# .env.example

# ── Chrome 用户数据目录 ──────────────────────────
# 默认使用项目内的 chrome_data 目录（已包含已登录的 Google 账号数据）
# Linux / macOS
CHROME_USER_DATA_DIR=/home/ubuntu/google_flow_mcp/chrome_data
# Windows（使用正斜杠或双反斜杠）
# CHROME_USER_DATA_DIR=C:/Users/YourName/google_flow_mcp/chrome_data

CHROME_PROFILE_DIRECTORY=Default

# Chrome 默认下载目录（留空则使用系统默认）
# Linux / macOS
# CHROME_DOWNLOAD_DIR=/home/ubuntu/Downloads/google_flow
# Windows
# CHROME_DOWNLOAD_DIR=C:/Users/YourName/Downloads/google_flow
CHROME_DOWNLOAD_DIR=

# Chrome 可执行文件路径（留空则由 DrissionPage 自动探测）
# Linux:   /usr/bin/google-chrome
# macOS:   /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
# Windows: C:/Program Files/Google/Chrome/Application/chrome.exe
CHROME_BINARY_PATH=

GOOGLE_FLOW_BASE_URL=https://flow.google.com
LOG_LEVEL=INFO

# browser_config.yaml 的路径（相对或绝对均可）
BROWSER_CONFIG_PATH=browser_config.yaml

# ── 多机分布式集群配置 (Cluster) ───────────────────
# 是否启用集群分发模式 (默认 true，单机也可开启无缝兼容)
IS_CLUSTER_ENABLED=true

# Master 节点配置 (主节点绑定地址与端口)
CLUSTER_MASTER_HOST=0.0.0.0
CLUSTER_MASTER_PORT=8765

# Worker 从机节点连接 Master 地址 (从机配置为 Master 局域网 IP，如 http://192.168.1.100:8765)
CLUSTER_MASTER_URL=http://127.0.0.1:8765

# Master 资产中心统一存储目录 (无需局域网共享文件夹，通过 HTTP 自动同步)
# CLUSTER_ASSET_DIR=data/assets

# 本机从机节点唯一标识 (建议区分，例如 worker_pc2, worker_laptop 等)
WORKER_ID=master_local_worker

# 本机 Chrome 登录的 Google Flow 账号标识 (用于区分不同账号配额)
WORKER_ACCOUNT=

